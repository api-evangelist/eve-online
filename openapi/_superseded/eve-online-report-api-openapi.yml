openapi: 3.2.0
info:
  title: Eve Online Report API
  version: 0.0.0
  description: 'Operations tagged Report across 2 of this provider''s published API definitions: parameter-enum-rendering.openapi.yaml, parameter-enum-rendering.swagger.yaml. Each path carries the servers of the definition it was published in.'
tags:
- name: Report
paths:
  /report:
    get:
      parameters:
      - in: query
        name: rel_date
        required: true
        schema:
          type: string
          default: today
          enum:
          - today
          - yesterday
          - lastweek
      responses:
        '200':
          description: OK
      tags:
      - Report
x-refined-from:
- parameter-enum-rendering.openapi.yaml
- parameter-enum-rendering.swagger.yaml
